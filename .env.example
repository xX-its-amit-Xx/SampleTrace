# Copy to .env (which is gitignored) and fill in your Benchling key.
# This file is the *template*; the real .env file should NEVER be committed.
#
# Alternative: use `sampletrace configure` to store the key in your OS
# keyring instead of a file. See docs/credentials.md.

BENCHLING_API_KEY=sk_paste_your_key_here
